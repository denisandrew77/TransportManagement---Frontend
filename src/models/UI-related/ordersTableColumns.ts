export interface orderTableColumn{
  orderNumber: number,
  plateNumber: string[],
  carrier: string[],
  clientNumber: string,
  client: string,
  loading: string[],
  delivery: string[],
  status: string,
  info: string[],
}
